//---------------------------------------------------------------------------

#ifndef CNotacionesH
#define CNotacionesH
#include "CP_CSMemoria.h"
//---------------------------------------------------------------------------
class Notaciones{
	private :
	Pila_CSMemoria *pila;
	public :
	Notaciones(Pila_CSMemoria*);
	Notaciones();
	double pos_Fija(AnsiString);
	double evaluar(int,char,int);
	bool esOperador(char);
	int prioridad(char);
	string invertido(string);
	string postfija(string);
	string prefija(string);
	bool parentesisOk(string);
};
#endif
